# Server Configuration
PORT=5000
NODE_ENV=development

# Client URL (for CORS validation)
CLIENT_URL=http://localhost:5173

# Database Connection (Sprint 4 Database Integration)
# Replace user, password, and dbname with your local PostgreSQL credentials
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/collabspace?schema=public"

# JWT Secret (Sprint 1 Authentication)
JWT_SECRET=supersecretjwtkeycollabspace2026
JWT_EXPIRES_IN=7d
